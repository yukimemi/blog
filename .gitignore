.DS_Store
.claude/settings.local.json
_import/
_import_from_zenn/
_site/
broken-links.txt
debug_import.ts
fix_dates.ts
fix_links.ts
import_zenn.ts
import_zola.ts
node_modules/

# Zola + postprocess build artifacts. `public/` is Zola's output directory,
# the direct counterpart to Lume's `_site/` above; jj otherwise tries to
# snapshot the copied-in images under public/images/ and warns on every one.
public/
.postprocess-cache.json
tools/postprocess/target/

# kata:gitignore:base:begin
# Editor / OS noise — kata-managed (yukimemi/pj-base).
.idea/
.vscode/
*.swp
*~
.DS_Store
Thumbs.db

# APM dependencies — install output of apm.yml's deps.
# Skill files under .agents/, .claude/, .gemini/ etc. are
# committed (kata seeds them; renri post_create refreshes via
# `apm install --update`); the apm_modules/ source cache is not.
apm_modules/
# kata:gitignore:base:end
